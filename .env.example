# Optional: Anthropic API key. Only needed if you're on API billing.
# Subscription users authenticate with `claude login` and can leave this blank.
# ANTHROPIC_API_KEY=your-key-here

# Optional: Server port (default: 3000)
# PORT=3000

# Optional: Log level (default: info)
# LOG_LEVEL=info
